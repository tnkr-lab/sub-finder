# Project Constraints

- **CRITICAL**: Do NOT alter, upgrade, or overwrite existing libraries currently present in the project.
- You are permitted to execute routine tasks (testing, linting, code generation) autonomously.
- If a task requires adding, upgrading, or modifying an existing dependency, you MUST halt and explicitly prompt the user for permission via text before generating the command.
